source 'https://rubygems.org'

gem 'sinatra'
gem 'json'
gem 'rack-contrib'
gem 'activesupport'
gem 'pathfinding'

group :development do
  # auto reload
  gem 'rerun'
  # WEBrick application server
  gem 'webrick'
end

group :production do
  # Use Passenger as app server
  gem 'passenger'
end
